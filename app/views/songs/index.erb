<h1>All Songs</h1>
<hr/>
 
 <% @songs.each do |song| %>
   <div class="song">
     <h3><%= song.title %> <small>(by: <%= song.author %>)</small></h3>
     <p>
       <%= song.url %>
     </p>
   </div>
 <% end %>